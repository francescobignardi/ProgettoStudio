# Diario francesco — 2026-07-14

## 10:41 — Prima route custom + apertura discorso viste Blade

> Bozza creata da claude. Da rivedere/integrare.

- Aggiunto `ports: "8080:8000"` al servizio php nel compose (host 8080 spostato per non collidere con Docker An-Soft). Container php ricreato, `artisan serve --host=0.0.0.0` in foreground, landing Laravel su `http://localhost:8080` da browser Mac. Serve `0.0.0.0` altrimenti dal container il server ascolta solo il loopback interno e dall'host è invisibile.
- Prima route custom `/ciao` in `routes/web.php`, poi evoluta da `return "hello world"` a `return view('helloworld', [...])` con array-dati (stringa + lista + dizionario). Toccato `php artisan route:list` (mappa 4 route base) e `php artisan list | grep make` (mappa generatori come "sommario del framework in comandi").
- View Blade `helloworld.blade.php` con `@if`, interpolazione `{{ $nome }}`, `@foreach` in modalità lista (`$colori as $colore`) e dizionario (`$ruoli as $persona => $ruolo`). Simmetria del `=>` tra costruzione e scomposizione dell'array assorbita in vivo.
- Due blocchi nuovi in `resources/appunti.md` ("Laravel — routing + artisan serve", "Blade — grammatica minima"). Fermato prima dei controller per consolidare — si riprende nel pomeriggio o domani con `make:controller`.
