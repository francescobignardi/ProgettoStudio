# Diary claude+francesco — 2026-07-01

## 10:23 — Avvio ProgettoStudio: direttive base e revisione `.claude` ereditato

**Intenzione (prima di agire)**
Francesco ha aperto la prima sessione su questo progetto. Richieste esplicite:
1. Ogni sessione va salvata in `resources/memory/` con le regole del `.claude`.
2. Il `.claude` va rivisto perché eredita da un progetto di lavoro diverso (GitLab An-Soft / Metis), mentre questo è studio personale destinato a **GitHub personale**.
3. Lo scopo del progetto è **istruire Francesco** in PHP e JavaScript con framework moderni. Sviluppatore da 2 anni, lavora molto con Claude Code e sente di non imparare abbastanza la "grammatica" dei linguaggi. Poco tempo, quindi si avanza a piccoli passi giornalieri, ma il progetto deve essere **complesso e bello** per finire sul GitHub personale come vetrina.
4. Scrivere in `resources/` un file `.md` con nome a mia scelta come punto di partenza e direttive base.

Piano per questa sessione:
- Leggere tutto il `.claude` (settings + comandi) per capire cosa è ereditato.
- Scrivere le direttive base in `resources/PROGETTO.md`.
- Salvare in auto-memory profilo utente, contesto progetto, e regola sul diary workflow.
- Aprire questo capitolo di diary.

**Esito (dopo)**
- Letti tutti i file del `.claude`: `settings.json` (hook diary generico, si tiene), `settings.local.json` (PROJECT_DEV=francesco corretto), e tutti i comandi. Classificati in: da tenere (`chiudi`, `ragiona`), da adattare (`pianifica`, `procedi`, `intervieni`), da riscrivere quando serviranno (`issue`, `stato`, `contesto`, `verifica` — tutti tarati su GitLab/Metis).
- Creato `resources/PROGETTO.md` come documento vivo con: scopo didattico, vincoli, target DA DECIDERE, stack DA DECIDERE, stato `.claude` ereditato, prossimi passi.
- Salvate 3 memorie in auto-memory: profilo utente, contesto progetto, feedback su diary workflow. Aggiornato `MEMORY.md`.
- Aperto questo diary di oggi (primo giorno del progetto).

**Prossimi passi da concordare con Francesco**
- Sessione di brainstorming per scegliere il **target/dominio** del progetto (§3 di PROGETTO.md).
- Subito dopo, scelta dello **stack** (§4).

## 10:34 — Estrazione documentazione da staincampo5 e scrittura primi standard

**Intenzione (prima di agire)**
Francesco ha chiesto di leggere tutta la documentazione del progetto di lavoro `/Users/francesco/Sites/atlas/staincampo5` (documentazione molto curata scritta dal suo capo) per estrarne il buono e portarlo qui in forma adattata, senza committare materiale altrui verbatim.

Piano:
- Delega a due agent Explore in parallelo — uno sugli standards, uno sull'impianto documentale/AI/diary — per non intasare il contesto.
- Presentazione a Francesco della sintesi con proposta di ordine di lavoro.
- Scrittura dei primi due file, tenendo il resto per quando servirà davvero.

**Decisioni prese con Francesco**
- **Rigore moderato** su Elegant Objects: prendiamo l'ossatura (fail-fast, immutabilità, CQRS, no getter/setter di default, no commenti superflui), lasciamo cadere i dogmi che collidono con framework moderni (Doctrine/Eloquent hanno getter/setter, DI containers usano reflection, ecc.).
- **Scriviamo solo 2 file adesso**, non i 6 dell'agent: `DOCUMENTATION.md` (mappa) e `STILE-CODICE.md`. Il resto (coding standards specifici per PHP/JS, testing, dependency rules, glossary) verrà quando ci sarà codice a cui applicarli.
- **File in inglese**, non italiano: il progetto va sul GitHub personale ed è più leggibile a chiunque.

**Esito (dopo)**
- Creato `resources/docs/DOCUMENTATION.md`: golden rule ("chi entra deve capire dove andare senza chiedere"), tre livelli L0/L1/L2 semplificati per progetto singolo, categorie trasversali (standards/decisions/architecture), sistema ADR append-only con titolo-affermazione, naming conventions, sistema diary, tabella "when to write what".
- Creato `resources/docs/standards/STYLE-GUIDE.md`: 12 sezioni (naming, costruttori/fail-fast, immutabilità, getter/setter, ereditarietà, null, commenti, tipi, reflection, framework-first, file/moduli, dipendenze) + grey areas da decidere dopo (testing, error handling, linting, frontend state). Nota d'origine onesta in fondo che riconosce da dove viene l'ispirazione (standard interno An-Soft del capo di Francesco) senza copiare verbatim.
- Creati le sottocartelle `docs/standards/`, `docs/decisions/`, `docs/architecture/` (vuote tranne standards).
- Aggiornato `PROGETTO.md` con §8 "Documenti già in essere" che elenca i due file, chiarisce l'origine e la scelta del rigore moderato. Rinumerato "Prossimi passi" come §9 e aggiunto il passo 6 (coding standards specifici post-scelta stack).

**Aperto / sospeso**
- Da fare (nel prossimo momento di lavoro): brainstorming per decidere target + stack. Da lì, coding standards specifici per PHP e JS che estendono STYLE-GUIDE.md.

## 10:45 — Brainstorming e scelta di stack + target

**Intenzione (prima di agire)**
Francesco ha proposto di invertire l'ordine: prima lo stack (guardando cosa è più ambito sul mercato del lavoro), poi il target. Ha già in mente una struttura "sito vetrina + sezione gestionale ordini con giacenze, tipologie, utenti a ruoli". Chiede orientamento sui framework PHP e JS più gettonati sul mercato.

**Panoramica fornita**
- PHP: **Laravel** (massima diffusione, ecosistema enorme, deploy semplice) vs **Symfony** (più rigoroso, "grammaticale", stipendi più alti ma curva più ripida). Il resto (Slim/CakePHP/CodeIgniter/WordPress) nicchia o non formativo.
- JS: **React** (standard di mercato) vs **Vue** (curva dolce, buono in IT), **Svelte** (nicchia in crescita, non da primo framework), **Angular** (enterprise, verboso). **TypeScript dato per scontato**.
- Meta-framework: Next.js (per React) o Nuxt (per Vue) sono il modo moderno di partire.
- Sul "link mostrabile a un recruiter": Vercel gratis per Next.js, Railway/Fly/VPS piccolo per Laravel. Fattibile.

**Decisione presa da Francesco**
- **Backend**: **Laravel**.
- **Frontend**: **React + Next.js in TypeScript**.
- **Target**: gestionale ordini (magazzino con giacenze, tipologie prodotto, ordini/storico, utenti con permessi differenziati). Estensioni future possibili. Del "posto" (hosting/deploy) non ci preoccupiamo ora.

**Esito (dopo)**
- Aggiornato `resources/PROGETTO.md`:
  - §3 "Target del progetto" da "DA DECIDERE" a "DECISO (2026-07-01)" con descrizione a due sezioni (vetrina + gestionale) ed elenco elementi minimi del gestionale.
  - §4 "Stack tecnologico" da "DA DECIDERE" a "DECISO (2026-07-01)" con dettaglio: Laravel + Next.js/React/TS + PostgreSQL + Tailwind + shadcn/ui + Sanctum + Docker Compose. Deploy e test framework rimandati.
  - §9 "Prossimi passi" spuntati i primi due punti e riscritto il resto con l'ordine concreto: nome progetto → ADR-0001 → modello dati → scaffold monorepo (`backend/`, `frontend/`, `docker/`) → git init → prima entità end-to-end (probabilmente Prodotto) → autenticazione → coding standards specifici post-codice.
- Aggiornata la memory `project_context.md` con decisioni prese e cose ancora aperte.
- Aggiornato `MEMORY.md` con la nuova one-liner.

**Aperto / sospeso**
- **Nome del progetto** — necessario per `git init` e cartella root.
- **Modello dati iniziale** — entità e relazioni principali (Utente, Ruolo, Prodotto, Categoria, Giacenza, Ordine, RigaOrdine, ...).
- **Struttura del monorepo** — proposta da presentare a Francesco prima di creare cartelle.
- **PHPUnit vs Pest** — rimandato a quando scriveremo il primo test.

**Prossimo passo concordato**
Presentare a Francesco una proposta di struttura del monorepo e un primo abbozzo di modello dati, prima di creare qualunque cartella o file.

## 10:54 — Correzione direzione: portfolio versatile, non gestionale

**Intenzione (prima di agire)**
Claude aveva risposto al brainstorming presentando una proposta di prossimi passi con: nome progetto, struttura monorepo, modello dati (con entità Utente/Ruolo/Prodotto/Categoria/Giacenza/Ordine/RigaOrdine), e 6 domande di dettaglio (ruoli semplici vs Spatie, giacenza semplice vs movimenti, chi fa gli ordini, primo ADR project-charter, ecc.).

**Correzione di Francesco**
Due punti importanti:
1. **Fraintendimento di fondo**: Claude stava leggendo il progetto come "gestionale ordini + vetrina di presentazione". La lettura corretta è l'opposto: **il progetto è un portfolio personale versatile ed eterogeneo**. Il gestionale è **una** delle sezioni tecniche, non "il progetto". Ce ne saranno altre in futuro.
2. **Troppe domande premature**: "È veramente presto! Non abbiamo ancora Docker, non abbiamo ancora deciso come e dove fare il DB (sarà MySQL), non c'è nulla, quelle verranno dopo." → Non entrare in dettagli tecnici (modello dati, ruoli, giacenze) prima che sia il loro momento.

Altre decisioni prese nello stesso messaggio:
- **Nome repo**: rimane `ProgettoStudio` (nome-contenitore, adatto a un portfolio eterogeneo).
- **Struttura**: monorepo ok.
- **DB**: **MySQL** (non PostgreSQL come Claude aveva ipotizzato).
- **Repo GitHub**: lo apre Francesco personalmente.

**Esito (dopo)**
- Aggiornato `resources/PROGETTO.md`:
  - §3 riscritto interamente: da "due sezioni (vetrina + gestionale)" a "portfolio versatile con più sotto-sezioni tecniche, di cui il gestionale è la prima". Rimossi tutti i dettagli specifici del gestionale (magazzino/tipologie/ordini/ruoli) — verranno decisi solo quando toccheremo quella sezione.
  - §4 corretto: DB da PostgreSQL a **MySQL**.
  - §7 aggiornato: nome repo `ProgettoStudio` confermato, GitHub aperto da Francesco.
  - §9 riscritto: prossimi passi ora sono "primo ADR di project charter (senza scendere nel gestionale)" → scaffold monorepo → sezione chi sono/vetrina → poi gestionale con dettagli decisi al momento → sezioni successive. Rimossa la voce "modello dati iniziale" come step separato: appartiene al momento in cui costruiamo il gestionale.
- Aggiornata memory `project_context.md` con la lettura corretta e un warning esplicito ("errore da non ripetere").
- Creata memory **`feedback_no_premature_details.md`** — regola operativa per non anticipare dettagli tecnici e per verificare il livello di comprensione prima di scendere. Aggiunta a `MEMORY.md`.

**Aperto / sospeso**
- Francesco apre il repo GitHub personale `ProgettoStudio`.
- Da fare in una prossima sessione: primo ADR di project charter (identità del portfolio + stack + motivazioni), poi scaffold minimo del monorepo.

**Prossimo passo concordato**
Attendere che Francesco apra il repo GitHub. Poi, alla prossima sessione, partiremo dallo scaffold minimo del monorepo e dall'ADR-0001. Nessun dettaglio del gestionale finché non ci arriviamo davvero.

## 10:57 — Primo commit: README, .gitignore, collegamento a GitHub

**Intenzione (prima di agire)**
Francesco ha creato il repo `https://github.com/francescobignardi/ProgettoStudio.git` (vuoto, senza README/gitignore/license). Ha deciso:
- **Memory dentro al repo** — il diary è parte del portfolio (mostra il processo di lavoro).
- **I comandi git li lancerà lui** dal terminale, con credenziali GitHub personali.
- **README lo scrivo io**.

Da fare qui:
1. Scrivere `README.md` breve ma curato — è la landing del portfolio, sia per un recruiter sia per la home GitHub.
2. Scrivere `.gitignore` con quel poco che serve ora (`.idea/`, `.DS_Store`); si estende quando arriveranno backend/frontend.
3. Preparare a Francesco la lista comandi terminale in ordine: `git init`, config nome/email locale, `git add`, primo commit, aggiungere remote, push.

**Esito (dopo)**
- Creato `README.md` in radice: presenta ProgettoStudio come portfolio personale in costruzione, con stack (Laravel + Next/React/TS + MySQL + Tailwind + shadcn/ui + Docker), layout del repo, dove leggere `PROGETTO.md`, `DOCUMENTATION.md`, `STYLE-GUIDE.md`, memory. Stato: Day 1 — 2026-07-01.
- Creato `.gitignore` con `.idea/`, `.vscode/`, `.DS_Store`, `.claude/settings.local.json`, `.env*`, placeholder per `node_modules/`, `vendor/`, `storage/`.
- Data guida passo-passo a Francesco per: `git init -b main`, config `user.name`/`user.email` **local** (non global) per evitare di sporcare identità cross-repo, `git add`, `git commit`, `git remote add origin`, `git push`.
- Adottati **Conventional Commits** de facto (primo commit: `chore: initial commit — project charter, documentation, README`).
- Emerso un problema: primo push fallito con **403** perché il Keychain di macOS aveva la credenziale HTTPS di `athenae-noctua-software` (residuo di qualcosa, non del lavoro corrente — il repo di lavoro `staincampo5` va su GitLab, non su GitHub). Guidato Francesco a `gh auth login` (browser flow) + `git credential-osxkeychain erase` per la voce `github.com`.
- **Push riuscito**: 24 oggetti, 27 KiB, branch `main` creato su GitHub. Repo online.
- Verifica finale:
  - Config git globale vuota (`user.name`/`user.email`/`credential.helper`) → nessuna identità di default che "segue" Francesco fuori da questo repo.
  - `git config --local` di `ProgettoStudio`: `Francesco Bignardi` / `f.bignardi@hotmail.it`.
  - Ultimo commit su GitHub firmato con quest'identità personale.
  - `gh auth status`: loggato **solo** come `francescobignardi` su github.com, token in keyring, scope `gist,read:org,repo,workflow`.
  - Keychain: **una sola** voce per `github.com` (username `francescobignardi`, token `gho_...`). La vecchia credenziale `athenae-noctua-software` è stata sovrascritta.
  - Repo di lavoro `staincampo5` punta a `atlas.an-soft.it` (GitLab), host separato, non toccato.
- **Separazione lavoro/personale confermata a Francesco** come primaria per lui (usa il PC aziendale anche in pausa). Segnalato apertamente il tema **non tecnico** della prudenza aziendale: verificare policy An-Soft su uso PC in pausa, consapevolezza di clausole IP/non-concorrenza, opportunità di comunicare al capo, e — come consiglio non urgente — spostare quando possibile lo sviluppo su hardware personale per pulizia definitiva.

**Cosa è aperto / sospeso**
- Email del commit: attualmente `f.bignardi@hotmail.it` (pubblica sul commit GitHub). Da decidere se passare all'email `noreply` di GitHub per privacy.
- Prossima sessione: **ADR-0001** (project charter) e/o primo scaffold minimo del monorepo. Nessun dettaglio del gestionale finché non si costruisce quella sezione.

**Prossimo passo concordato**
Chiusura sessione di oggi con `/chiudi` (poi rimandata — vedi capitolo successivo).

## 11:59 — Primo micro-esercizio: classe Product

**Intenzione (prima di agire)**
Francesco ha fatto autovalutazione: "conosco la teoria (OOP, array, foreach), ma a memoria scrivere codice PHP/JS è difficile". Diagnosi: gap di fluenza/memoria muscolare, non gap di teoria.

Piano proposto e accettato: **doppio binario** — (A) fluenza sui linguaggi puri con micro-esercizi, (B) Laravel/React in parallelo ma più lento. Prima cosa: micro-esercizio di calibrazione.

Consegna data: classe `Product` con nome + quantità, costruttore, metodo `isAvailable()` che ritorna `true` se quantità > 0. In un file `run.php` due prodotti (qty 5 e qty 0), stampa dei due `isAvailable()`. **Senza Google.**

Francesco ha scritto `Product.php` e `run.php` in root del progetto (visibile in IDE). Ora Claude va a leggere.

**Esito (dopo)**

Il micro-esercizio ha rivelato un panorama molto più ricco di quanto Francesco stesso immaginasse. Riassumo:

- **Prima stesura** — Francesco ha scritto `Product.php` (classe con `$name`, `$quantity`, costruttore, `isAvailable($quantity)`) e `run.php` (istanzia 2 prodotti, chiama il metodo, stampa `true`/`false`). Codice funzionalmente vicino al corretto, ma con un **bug concettuale importante**: `isAvailable()` prendeva un parametro `$quantity` invece di leggere `$this->quantity` — quindi il metodo ignorava lo stato dell'oggetto. Fatto notare da Claude che l'IDE segnalava "manca parametro" quando Francesco provava a chiamare `isAvailable()` senza argomenti.
- **Domanda socratica** posta ("perché passi `$product1` di nuovo? L'oggetto non ha già la sua quantità dentro?"). Francesco ha risposto che l'IDE lo guidava verso il parametro e che togliendolo `$quantity` dava errore.
- **Spiegazione grammaticale** su `$this`, sui due `$quantity` con lo stesso nome ma nature diverse (proprietà vs parametro locale), sul fatto che l'IDE aiuta a rispettare la firma ma non a valutarla.
- **Riscrittura corretta** — Francesco ha rimosso il parametro da `isAvailable()`, sostituito `$quantity` con `$this->quantity`, aggiornato `run.php` di conseguenza. **Dichiarazione importante di Francesco**: *"Per la prima volta mi pare di aver capito il senso del $this"*. Questa è la vittoria concettuale del giorno.
- **Esecuzione da terminale** — Francesco non padroneggiava files `.php` da CLI. Guidato attraverso: `require 'Product.php'` in `run.php`, spiegazione della CLI di PHP, comando `php --version`.
- **Errore `dyld`** su libreria ICU mancante (PHP 8.2.5 di Homebrew rotto per aggiornamento a valle di icu4c). Spiegato cosa fosse `dyld`, ICU, e il pattern Homebrew-dependency-drift. Soluzione: `brew reinstall php`.
- **Errore `Permission denied`** su `/usr/local/etc/odbc.ini` prima e `/usr/local/etc/freetds.conf` poi — file `/usr/local/etc/*` di proprietà di root (residuo di vecchi `sudo`). Passati al fix ricorsivo con `sudo chown -R $(whoami) /usr/local/etc /usr/local/var /usr/local/Cellar`.
- **Reinstallazione riuscita**, `php --version` funzionante.
- **Primo lancio da terminale**: Francesco ha ricordato *a memoria* il comando `php run.php` senza cercare su Google. Output: `truefalse%`. Spiegato il significato della `%` finale di zsh (mancata newline).
- **Micro-esercizio bonus**: Francesco ha modificato `run.php` etichettando le stampe (`Product1: true`, `Product2: false`). Approccio semplice e onesto — invece della concatenazione o interpolazione, ha usato quattro echo distinti. Ha applicato spontaneamente la regola "prima il verde, poi il refactor". Le newline mancano ancora ma è stato segnalato come nota futura, non da correggere adesso.
- **Docker come priorità futura**: Francesco l'ha richiamato spontaneamente dopo aver vissuto sulla sua pelle problemi di ambiente locale. Perfetto tempismo per introdurlo prossimamente — ha sentito il dolore che Docker risolve, non lo imparerà come "un altro tool", lo imparerà come "risposta a qualcosa che ho sofferto".
- **Commit degli esercizi**: Francesco ha ricordato a memoria `git add . && git commit -m "..." && git push`. Discusso se committare esercizi che verranno spostati/rimossi in futuro. Deciso di committarli: (a) il repo è anche diario dell'apprendimento, (b) git conserva la storia comunque, (c) al recruiter una storia di commit fitta racconta più di una vetrina pulita. Introdotto il prefisso Conventional Commit custom **`learn:`** per gli esercizi didattici — se Francesco lo adotta, va salvato come convenzione.
- Alla chiusura, `/chiudi` non è stato riconosciuto dal CLI (probabile refresh comandi da fare a inizio sessione futura, o percorso di cache dei comandi da verificare). Chiusura eseguita manualmente da Claude seguendo la procedura di `.claude/commands/chiudi.md`.

**Aperto / sospeso**
- Perché `/chiudi` non è stato riconosciuto — da verificare in una futura sessione (riavvio Claude Code CLI, o troubleshooting sui commands custom).
- Newline mancanti in `run.php` — non correggere ora, è nota futura.
- Email dei commit: `f.bignardi@hotmail.it` — decidere se passare a `noreply` GitHub per privacy.
- Convenzione `learn:` per commit di esercizi — se Francesco la usa oggi, va formalizzata in `STYLE-GUIDE.md` o in un piccolo ADR.
- Docker come priorità imminente nel piano didattico.
- **Pianificazione del percorso didattico generale** — Francesco l'ha rimandata a una prossima sessione. Con il livello reale ora visibile (OOP di base solido, esecuzione da terminale nuova, gap di fluenza confermato), Claude può proporre un curriculum tarato.

### Chiusura argomento — 14:40

- **Cosa è stato deciso**: (1) Claude è tutor/insegnante senior, Francesco scrive tutto il codice; regola cardine sovraordinata alle altre e salvata in memory. (2) Doppio binario didattico — fluenza sui linguaggi puri (micro-esercizi PHP/JS) in parallelo a Laravel/React. (3) Esercizi si committano nel repo (parte del diario di apprendimento). (4) Docker priorità imminente. (5) Conventional Commits adottati, con eventuale prefisso custom `learn:` per esercizi.
- **Cosa è stato fatto**: (1) Ricostruito il vero senso del progetto dopo che Claude aveva scivolato in "documentatore-per-Francesco". (2) Primo micro-esercizio PHP eseguito interamente da Francesco (classe, `$this`, require, CLI). (3) Risolti problemi di ambiente Homebrew (dyld, permessi). (4) Primo `php run.php` a memoria, output `truefalse` e poi versione etichettata. (5) Salvate 4 memorie di regola/contesto: `feedback_tutor_role.md` (⭐ cardine), `project_work_boundary.md`, `feedback_no_premature_details.md`, `reference_staincampo5.md`. (6) `Product.php` e `run.php` creati, da committare.
- **Aperto / sospeso**: pianificazione curriculum didattico, docker imminente, commit degli esercizi da fare, `/chiudi` slash-command non riconosciuto (da diagnosticare), newline in run.php, email commit privacy, formalizzazione `learn:`.
- **Prossimo passo concordato**: Francesco committa `Product.php` e `run.php` con `learn:` come prefisso (se lo sceglie). Poi chiusura effettiva della sessione. Alla ripresa: pianificazione generale del percorso didattico + Docker.

## 11:47 — Svolta: Claude è tutor, Francesco scrive il codice

**Intenzione (prima di agire)**
Francesco richiama il vero scopo del progetto, ricordato all'inizio della giornata ma non applicato durante la sessione. Il progetto esiste perché **al lavoro il ~99% del codice lo scrive Claude** e Francesco sta atrofizzando le sue capacità pratiche. In pausa vuole allenarsi scrivendo *lui* il codice. Il repo GitHub è un byproduct, potrebbe anche non esistere. Il valore è nel gesto di scrivere, non nel risultato.

Nella giornata Claude ha invece scritto **tutti i primi documenti** (DOCUMENTATION.md, STYLE-GUIDE.md, README.md, .gitignore) e ha servito comandi git pronti. Francesco non ha battuto una riga di codice. È esattamente il pattern che il progetto deve rompere.

**Nuovo patto operativo (concordato con Francesco)**

Claude è **insegnante/tutor senior**, non collega. Francesco è studente **da zero** su Laravel e React. Pianificazione dal generale al particolare in step sempre più piccoli, così Francesco vede sempre insieme il quadro e il passo del momento.

Prima di ogni pezzo di codice, Claude fornisce:
1. Il **perché** — dove siamo nel piano, perché ora.
2. La **grammatica di base** — concetti nuovi partendo dai linguaggi (PHP/JS puri) e salendo ai framework. Esempi piccoli per capire, non per copiare.
3. Il **compito** atomico e verificabile.
4. **Cosa aspettarsi** — errori tipici, come leggere i messaggi.

Poi Francesco scrive. Claude legge e dà feedback. Se blocca: prima domande socratiche, poi indicazioni sempre più specifiche, mai partire dalla soluzione. **Prima il verde poi il refactor**: se scrive qualcosa che funziona ma non è idiomatico, glielo si dice *dopo* che funziona.

Documentazione, config, README, gitignore, ADR: **li scrive Claude** — non sono gesto didattico, per Francesco sarebbe perdita di tempo. Codice applicativo (`.php`, `.blade.php`, `.js`, `.jsx`, `.ts`, `.tsx`, migrations, seeder, test): **lo scrive Francesco**, sempre.

**Documenti già scritti oggi**: si tengono, sono cornice. La regola vale da qui in poi, piena.

**Esito (dopo)**
- Creata memoria `feedback_tutor_role.md` con la regola cardine e dettagliata: cosa fa Claude, cosa non fa, segnali di allarme di scivolamento, frontiera dei "documenti" (bianco/nero/grigio), check di fine sessione. Marcata come regola sovraordinata alle altre.
- Aggiornato `MEMORY.md` con la voce in cima e stella ⭐ per farla saltare all'occhio.
- Registrato in questo diary come **svolta morale** della giornata.

**Aperto / sospeso**
- Alla prossima sessione: Francesco fa una **autovalutazione onesta** su cosa sa di PHP puro (classi/ereditarietà/interfacce/namespace/composer, array, mai scritto migration/controller/model altrove?) e JS puro (`let/const`, closure/arrow, Promise/async, moduli, TypeScript). Serve a Claude per tarare da che livello partire — non sprecare tempo su cose già chiare né dare per scontato cose ignote.
- **Poi**: pianificazione generale del percorso didattico. Dal grande al piccolo. Prima "cosa impariamo prima, cosa dopo" a livello di curriculum, poi step atomici quando arriviamo al momento di scrivere.
- Aperto anche: email del commit hotmail vs noreply GitHub (già segnalato).

**Prossimo passo concordato**
Chiusura sessione di oggi con `/chiudi`.
