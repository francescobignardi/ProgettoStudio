# Diario francesco — 2026-07-23

## 15:32 — CRUD: la C (Create) — form + validazione + store

> Bozza creata da claude. Da rivedere/integrare.

- Chiusa la lettura con `orderBy` (ordinamento esplicito: senza `ORDER BY` l'ordine non è garantito) e completata la **C** di CRUD: form Blade (`@csrf`) → `store` con validazione di dominio → `Product::create` → redirect.
- Imparato: `for`/`id`/`name` nel form (solo `name` parla col server); `action`/`method`; POST-Redirect-GET (il redirect evita il doppione su F5); `return` obbligatorio anche sul redirect.
- Debug/decisioni da solo: bug ordine route (`{id}` cattura `"create"`), e capito che per *cambiare pagina* basta `<a href>`, non un form + route dedicata ("cannone per una mosca").
- Claude ha sbagliato due previsioni (make:view, il 419 CSRF su Laravel 13): lezione = si verifica coi fatti, non si indovina.
- Appunti: nuovo capitolo CRUD/Create + nota `orderBy`. README aggiornato. Prossimo: Update e Delete.
