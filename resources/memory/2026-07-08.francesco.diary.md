# Diario francesco — 2026-07-08

## 11:51 — Chiusura giornata: appunti Docker, README onesto

> Bozza creata da claude. Da rivedere/integrare.

- Chiusa Fase 3 (Docker Compose) al primo tentativo: scritto `docker-compose.yml` con due servizi (php + mysql), acceso lo stack con `docker compose up -d`, entrato nel container php e verificato che `php run.php` desse lo stesso output del Mac.
- Applicate le tre pratiche fondanti previste per non collidere con i Docker di lavoro An-Soft: prefisso `progettostudio-` sui container, porta MySQL su 3316, reti separate (default di Compose).
- Chiarita — e non era scontata — la distinzione **Docker (strato environment) vs Composer (strato dipendenze)**: risolvono problemi diversi, non sono alternativi. Anche il pezzo delle librerie phar An-Soft l'ho letto meglio: quello era un problema Composer, non Docker.
- Aggiunto blocco Docker/Compose in `resources/appunti.md`; riscritte tre sezioni del `README.md` con checklist a 5 fasi che rende visibile il percorso.
