# Diario francesco — 2026-07-24

## 09:00 — Ripresa contesto + questione git (branching)

> Bozza creata da claude. Da rivedere/integrare.

- Deciso: niente branch ora (mono-sviluppatore, nessun deploy). Quando ci sarà una versione stabile → `main` + **tag di release** + branch di feature usa-e-getta, non due branch fissi produzione/lavoro.

## 11:37 — CRUD: U (Update) e D (Delete) — completamento del ciclo

> Bozza creata da claude. Da rivedere/integrare.

- **CRUD completo**: aggiunte U (edit + update) e D (destroy). Ciclo index/create/store/show/edit/update/destroy chiuso.
- Imparato **method spoofing**: il form HTML fa solo GET/POST, `@method('PUT'/'DELETE')` + `_method` fanno riscrivere il verbo a Laravel. REST: stesso URL `/products/{id}`, il verbo decide l'azione.
- Lezione grossa `::` vs `->`: statico (`create`, `destroy`) vs istanza (`update`); scelto `findOrFail` + `->update()` per separare trova/modifica e tenere la guardia 404. Diagnosi fatta da solo.
- Delete = form (non `<a>`): il button non invia da solo. Due commit `learn:` fatti; appunti (capitolo U+D) e README aggiornati.
