# Diario francesco — 2026-07-01

## 11:59 — Primo micro-esercizio: classe Product

> Bozza creata da claude. Da rivedere/integrare.

- Scritto la prima classe PHP (`Product.php`) e uno script che la usa (`run.php`) senza copiare da nessuna parte.
- Fatto un errore concettuale su `$this` e sui parametri di metodo: il metodo `isAvailable` prendeva `$quantity` dall'esterno invece di leggere lo stato dell'oggetto. Corretto dopo una domanda socratica di Claude — **e per la prima volta ho capito davvero cosa è `$this`**.
- Passato attraverso rottura di PHP di Homebrew (dyld / ICU) e permessi `/usr/local/etc` di root — risolto con `brew reinstall php` + `sudo chown -R`.
- Lanciato per la prima volta uno script PHP dalla CLI **a memoria**: `php run.php` → `truefalse`. Poi versione etichettata `Product1:true` / `Product2:false`.
- Deciso col tutor: da qui in poi lui NON scrive codice, io lo scrivo tutto. Lui insegna, io batto.