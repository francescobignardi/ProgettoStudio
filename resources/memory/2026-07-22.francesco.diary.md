# Diario francesco — 2026-07-22

## 11:xx — Triangolo MVC chiuso con dati reali (ProductController + view)

> Bozza creata da claude. Da rivedere/integrare.

- Creata la prima fetta Eloquent completa: migrazione `products` + model `Product` (`$fillable`) + `ProductSeeder`, poi `ProductController::index()` con `Product::all()`, route `/prodotti`, view Blade → i 4 prodotti veri dal DB in pagina.
- Imparato: `decimal` (non `float`) per i prezzi; i seeder **non hanno memoria** (accumulano); `migrate:fresh --seed` = reset del mondo (mai in prod).
- Debug fatto da solo: dal JSON grezzo alla freccia `->` sugli oggetti; poi `@forelse/@empty` al posto di `@foreach`+`@isset` (difendersi dalla lista vuota, non da un dato garantito).
- Toccato il primo principio in versione soft: coesione / un controller per argomento.
- Appunti: nuova cheat sheet di avvio + blocco Eloquent con mappa del flusso del dato. README aggiornato.

## 12:4x — Pagina di dettaglio singolo prodotto (show + findOrFail)

> Bozza creata da claude. Da rivedere/integrare.

- Extra pomeridiano: filtro `where('stock','>',0)->get()` (lista solo prodotti in giacenza, Monitor sparisce) + pagina dettaglio `/products/{id}` con metodo `show`.
- Imparato: `where` costruisce, `->get()` esegue; `find`/`findOrFail` per la singola riga (findOrFail = trova o 404).
- Debug da solo: `view('product', [$product])` (indicizzato) → "Undefined variable", risolto con `['product' => $product]` (associativo). Grammatica PHP.
- Deciso insieme: la guardia "prodotto inesistente" va nel **controller**, non in Blade (controller decide, view presenta; risorsa mancante = 404). Usato `findOrFail`.
- Appunti estesi (where/get/find/findOrFail, route con parametro, array indicizzato vs associativo). README aggiornato.
