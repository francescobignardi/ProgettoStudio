# Diario francesco — 2026-08-04

## 17:29 — Ripresa contesto: si riparte da A.2 (Supplier 1-a-molti PurchaseOrder)

> Bozza creata da claude. Da rivedere/integrare.

- Creata la prima relazione Eloquent vera: `Supplier` **hasMany** `PurchaseOrder`, `PurchaseOrder` **belongsTo** `Supplier`.
- Migration `purchase_orders` con FK `supplier_id` (`foreignId->constrained->restrictOnDelete`); `status` default `draft`. Ragionata la testata minima (scremati i campi/FK da gestionale grosso).
- Model + controller (`index/show/create/store`) + route resource + seeder (dipendenza risolta nell'ordine del `DatabaseSeeder`).
- Verificata la relazione in **Tinker** (`->supplier` restituisce l'oggetto Supplier).
- View index/show + form `create` con `<select>` fornitori (concetto nuovo: `value`=id per la macchina, testo=name per l'umano). Ordine creato dal browser: funziona.
- Da rifinire domani: input `notes` + bottone submit nel form; poi commit (chiedere messaggi a claude).
