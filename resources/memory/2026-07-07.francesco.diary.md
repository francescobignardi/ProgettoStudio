# Diario francesco — 2026-07-07

## 11:52 — Fase 1, esercizio 2: metodo che ritorna una collezione filtrata

> Bozza creata da claude. Da rivedere/integrare.

- Scritto un secondo micro-esercizio PHP: aggiunto `availableProducts(): array` a `Warehouse` (con docblock `@return Product[]`) e un `getName()` in `Product` per stampare la lista dei disponibili.
- Sono arrivato da solo al concetto di **getter** ragionandolo: ho capito che mettere `$name` public non era la strada giusta e ho chiesto conferma prima di scriverlo. Confermata la piccola verità: la scuola Yegor mi aveva fatto dimenticare getter/setter, ora vanno riattivati per Laravel.
- Anche oggi due errori risolti da solo (`return` al posto di `echo`, poi le newline mancanti) — nessun aiuto, solo rileggere e riprovare.
- Deciso oggi il piano didattico ufficiale: **PHP-first** in 5 fasi (fluenza → Composer → Docker → Laravel → JS/TS/React); JS/TS rimandato a quando servirà il frontend.
