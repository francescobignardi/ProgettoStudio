# Diario francesco — 2026-07-28

## 10:16 — Roadmap operativa (ROADMAP-GESTIONALE.md)

> Bozza creata da claude. Da rivedere/integrare.

- Deciso: si parte col gestionale **backend** (relazioni Eloquent), React/TS dopo.
- Scremato un gestionale reale (`vm-schema`) → scritto lo scheletro nostro generalizzato in `PROGRAMMA-GESTIONALE.md` (mezzo ciclo acquisto→magazzino, tripletta prodotto/articolo/ponte, ruoli rimandati).
- Aggiunto obiettivo "usabile lato client": frontend React/Next come vetrina, Blade minimale, API REST come contratto; seed realistici.
- Creata `ROADMAP-GESTIONALE.md` con capitoli A–D a passi atomici e conteggi onesti (niente percentuali finte).
- Prossimo passo: **A.1** — `Supplier` (migration + model + CRUD minimo).

## 15:16 — Refactor view a cartelle + caccia al bug redirect + decisione lingua EN

> Bozza creata da claude. Da rivedere/integrare.

- **A.1 fatto (da solo!)**: `Supplier` con migration + model + CRUD minimo, `Route::resource->only`, seeder. Sciolti 5 intoppi da solo (`all()` vs builder, `code` senza default, 404 su `find` per code vs id, `int`→`integer`).
- Refactor view in **cartelle per entità** (`products/`, `suppliers/`) con nomi resource + `view()` a notazione a punto.
- **Bug redirect capito bene**: `view()` = notazione a punto (nome view); `Redirect::to`/`href`/`action` = URL con slash. Mai il punto negli URL → dava 404.
- Deciso: tutto il progetto in **inglese** (UI + seed).
- Prossimo passo: **A.2** — prima relazione `Supplier` ha molti `PurchaseOrder` (`hasMany`/`belongsTo`).
